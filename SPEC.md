# Babos — 規格與決策紀錄

最後更新：2026-08-14

> 這份文件是給「明天在另一台電腦繼續做這件事的人」看的。
> 包含所有已定案的決策**與被否決的方案**——否決的部分特別重要，
> 不然很容易繞回去重新討論一遍。
>
> **這裡寫的是「為什麼長這樣」。踩過的坑與實作細節在 [HANDOFF.md](HANDOFF.md)，
> 現況與怎麼跑起來在 [README.md](README.md)。**

規格已全數實作完畢，目前 v0.1.6。下面的內容維持當初決策時的原文，
只在明顯過時處補上實作結果。

---

## 這是什麼

個人用的工作記錄 app。一個案子一筆記錄，桌面尺寸（約 1600px 寬）。

**核心目的不是管理專案，是留下痕跡。** 使用者是設計師，一個人用，案子量級是幾十筆。
不需要協作、不需要甘特圖、不需要資料庫。

---

## 三個區塊

### 上半左 — 氣泡區（這個 app 的招牌）

一顆氣泡＝一個案件，在區域內漂浮並互相碰撞，像氣球。

**兩個編碼互相獨立：**

```
尺寸 ＝ 這件案子現在佔掉多少腦袋（隨進度走常態曲線）
size = 30 + scale × 130 × (0.35 + 0.65 × sin(progress × π))
        剛開始小 → 中期最大 → 收尾又變小
        scale 是案子本身的規模，讓大小案有基本差異

深淺 ＝ 完成度（越做越深）
opacity = 0.18 + 0.82 × progress
        剛建立最淡 → 完成前最深
```

點擊選取 → 右側卡片切換。hover 顯示案件名。大顆的氣泡直接顯示名稱，小顆的不顯示。

**排列用 d3-force**（`forceCollide` ＋ 弱 `forceCenter`）。要填滿整個區域，
不能沉到底部，也不能蓋到上方的篩選 chip 與下方的圖例。

### 上半右 — 詳細卡片

- 案件名、建立日期
- **進度：1–10 的節點，可點擊推進**。這是操作不是顯示——點下去要即時改變對應氣泡的尺寸與深淺
- **作業日誌**：時間倒序，底部常駐一行輸入框
- **連結**：多筆，每筆有類型標籤（Drive／Figma／本機路徑／URL）。本機路徑用 `file://`

**未選取時直接顯示「最後更新的案件」**，不做教學型空狀態。詳見下方決策 3。

### 下半 — 列表

- 左側欄：依狀態與客戶篩選。**篩選同時作用於氣泡區與列表**，兩邊永遠是同一批資料的兩種呈現
- 主表：案件名、進度（小圓點＋百分比數字）、狀態、最終更新、連結數
- 預設按最終更新倒序，最新自動置頂並短暫高亮
- **點列＝進入完整專案頁**（與點氣泡的快速預覽不同）

---

## 完成流程

推到第 10 個節點並確認後：

1. 氣泡**先短暫漲大、變全黑**（最後一次呼吸）
2. 往上飄走，同時縮小、淡出
3. 畫面留下一個空位，其他氣泡重新排列補上
4. 案件移入「完了」，仍可從列表查閱

**這是整個 app 唯一該有儀式感的時刻，其他地方都應該安靜。**

---

## 決策紀錄（含被否決的方案）

### 決策 1：進度環 → 已否決，改用透明度

**否決的方案**：在氣泡外圈加進度環表示完成度。

**否決理由**：視覺太雜，實心圓比帶環的圓更像氣球。透明度已經能承擔這個資訊。

**注意**：不要在氣泡上加環、加框、加百分比標籤。尺寸與深淺承擔全部資訊，
氣泡必須維持乾淨的實心圓。

### 決策 2：透明度遞減 → 已否決，改成遞增

**否決的方案**：剛開始 100% 深，越做越淡，完成時變成 0% 然後消失。

**否決理由**：兩個編碼要各自獨立，不能同方向疊加。

**尺寸的真正意義（用戶原話）**：

> 「我作為一個設計師，最頭痛的其實是設計中段的過程，快要結束的時候反而不重要了，
> 所以我才會跟你說他應該是一個常態曲線的大小，因為快結束的時候反而不重要」

所以尺寸表達的是**現在吃掉多少注意力**——中段最耗神所以最大，收尾階段剩下的
多半是機械性的動作，不需要再佔版面。

**濃淡則表達「已經走了多遠」**，與注意力無關，所以隨進度單調遞增。

兩者組合後的讀法：

| 視覺 | 意思 |
|---|---|
| 小而淡 | 剛開始，還沒進入狀況 |
| 大而中 | 正在燒腦，最需要你 |
| 小而深 | 快好了，可以放心 |

如果濃淡跟著尺寸一起衰減，「已經走了多遠」這個維度就消失了，
畫面上會分不出「剛開始」和「快結束」——兩者都是又小又淡。

### 決策 3：教學型空狀態 → 已否決

**否決的方案**：未選取時顯示「點擊氣泡查看詳細」＋本週更新的捷徑。

**否決理由**：那種提示只有第一次有用，之後每次打開都在浪費一整塊版面。

**改成**：未選取時直接顯示**最後更新的案件**。打開這個 app 的典型情境就是
「繼續做昨天那件事」，直接顯示正好命中。

兩條配套定義：

- **「最後更新」＝最後被寫入的**（推進度、加日誌、改連結），**不含單純點開看過**。
  否則瀏覽一輪之後卡片會跟著滑鼠亂跳
- **每次開啟一律顯示最後更新的，不要記憶上次選取的**。上次點的可能只是隨手看看

**真正的空狀態**只有零個案件時才出現：空表格 ＋ 明顯的「＋新規案件」。

### 決策 4：原始 wireframe 缺的東西（已補）

- 新增案件的入口（頂列「＋新規案件」）
- 空狀態的定義（見決策 3）
- 進度節點是可操作的，不只是顯示

---

## 實作建議與最終結果

| 項目 | 當初的建議 | 實際做成什麼 |
|---|---|---|
| 氣泡排列 | **d3-force**。自己寫碰撞會做很久且不穩 | **自己寫了**。為了維持單一 HTML 檔零依賴——CDN 會在離線時壞掉，內嵌會讓檔案肥大。12 筆規模自己寫反而輕。詳見 HANDOFF 2.4 |
| 資料儲存 | 本機 JSON 就夠。一個人用、幾十筆，不需要資料庫 | 照做。`~/Library/Application Support/com.shihbo.worklog/work-tracker.json`，localStorage 留一份當保險 |
| 打包成 app | **Tauri**（已有星鑑日誌的經驗，坑踩過了） | 照做，但還是踩了新坑（外掛沒註冊、`confirm()` 是 async、`setFullscreen` 會 crash）。詳見 HANDOFF |
| 氣泡數量 | 設上限。只顯示「進行中＋待開始」，完成的沉進列表 | 照做（完成的不進氣泡區），但沒有硬上限 |
| 介面語言 | 日文 | 做成三語可切換（英／日／繁中），預設英文 |
| 視覺風格 | 尚未決定。方向是安靜、低對比，讓氣泡區成為唯一的視覺焦點 | 套用 Ameba（午夜藍），另備灰階配色。token 見 `DESIGN.md` |

---

## 最重要的一件事

**這個 app 的成敗不在視覺，在會不會持續記。**

記錄類工具的死法都一樣：做得漂亮，用兩週就停。差別只在「記一筆」要花幾秒。

所以**日誌那個常駐的一行輸入框是整個介面最重要的元件**——它是唯一每天都會碰的地方。
不要讓它被擠到角落，不要做成要先點「新增」才展開，不要要求填標題或分類。

打開、打字、Enter。三個動作。

---

## 檔案

- `docs/wireframe.html` — 可互動的 wireframe，含編號規格說明。截圖給 Claude Design 當參考圖用
- `docs/claude-design-prompt.md` — 可直接複製貼進 Claude Design 的英文 prompt
- `docs/reference-ameba.jpg` — 視覺風格的來源截圖
- `SPEC.md` — 本檔
