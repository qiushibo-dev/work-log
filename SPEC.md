# 工作記錄 App — 規格與決策紀錄

最後更新：2026-08-13

> 這份文件是給「明天在另一台電腦繼續做這件事的人」看的。
> 包含所有已定案的決策**與被否決的方案**——否決的部分特別重要，
> 不然很容易繞回去重新討論一遍。

---

## 這是什麼

個人用的工作記錄 app。一個案子一筆記錄，桌面尺寸（約 1600px 寬）。

**核心目的不是管理專案，是留下痕跡。** 使用者是設計師，一個人用，案子量級是幾十筆。
不需要協作、不需要甘特圖、不需要資料庫。

---

## 三個區塊

### 上半左 — 氣泡區（這個 app 的招牌）

一顆氣泡＝一個案件，在區域內漂浮並互相碰撞，像氣球。

**兩個編碼互相獨立：**

```
尺寸 ＝ 當前投入強度（隨進度走常態曲線）
size = 30 + scale × 130 × (0.35 + 0.65 × sin(progress × π))
        剛開始小 → 中期最大 → 收尾又變小
        scale 是案子本身的規模，讓大小案有基本差異

深淺 ＝ 完成度（越做越深）
opacity = 0.18 + 0.82 × progress
        剛建立最淡 → 完成前最深
```

點擊選取 → 右側卡片切換。hover 顯示案件名。大顆的氣泡直接顯示名稱，小顆的不顯示。

**排列用 d3-force**（`forceCollide` ＋ 弱 `forceCenter`）。要填滿整個區域，
不能沉到底部，也不能蓋到上方的篩選 chip 與下方的圖例。

### 上半右 — 詳細卡片

- 案件名、建立日期
- **進度：1–10 的節點，可點擊推進**。這是操作不是顯示——點下去要即時改變對應氣泡的尺寸與深淺
- **作業日誌**：時間倒序，底部常駐一行輸入框
- **連結**：多筆，每筆有類型標籤（Drive／Figma／本機路徑／URL）。本機路徑用 `file://`

**未選取時直接顯示「最後更新的案件」**，不做教學型空狀態。詳見下方決策 3。

### 下半 — 列表

- 左側欄：依狀態與客戶篩選。**篩選同時作用於氣泡區與列表**，兩邊永遠是同一批資料的兩種呈現
- 主表：案件名、進度（小圓點＋百分比數字）、狀態、最終更新、連結數
- 預設按最終更新倒序，最新自動置頂並短暫高亮
- **點列＝進入完整專案頁**（與點氣泡的快速預覽不同）

---

## 完成流程

推到第 10 個節點並確認後：

1. 氣泡**先短暫漲大、變全黑**（最後一次呼吸）
2. 往上飄走，同時縮小、淡出
3. 畫面留下一個空位，其他氣泡重新排列補上
4. 案件移入「完了」，仍可從列表查閱

**這是整個 app 唯一該有儀式感的時刻，其他地方都應該安靜。**

---

## 決策紀錄（含被否決的方案）

### 決策 1：進度環 → 已否決，改用透明度

**否決的方案**：在氣泡外圈加進度環表示完成度。

**否決理由**：視覺太雜，實心圓比帶環的圓更像氣球。透明度已經能承擔這個資訊。

**注意**：不要在氣泡上加環、加框、加百分比標籤。尺寸與深淺承擔全部資訊，
氣泡必須維持乾淨的實心圓。

### 決策 2：透明度遞減 → 已否決，改成遞增

**否決的方案**：剛開始 100% 深，越做越淡，完成時變成 0% 然後消失。

**否決理由**：尺寸在收尾階段已經變小，如果深淺同時變淡，
**快做完的案子會變成畫面上最不顯眼的東西**。

但「剩最後 10% 卻卡住」正是最容易被遺忘的狀態，它應該跳出來煩人，不該消失。

**改成遞增之後**：收尾中的案子是「小而深」——一個濃縮的深點，反而會抓住視線。
隱喻上也順：氣球越吹越飽和明顯，放掉之後才消失。

### 決策 3：教學型空狀態 → 已否決

**否決的方案**：未選取時顯示「點擊氣泡查看詳細」＋本週更新的捷徑。

**否決理由**：那種提示只有第一次有用，之後每次打開都在浪費一整塊版面。

**改成**：未選取時直接顯示**最後更新的案件**。打開這個 app 的典型情境就是
「繼續做昨天那件事」，直接顯示正好命中。

兩條配套定義：

- **「最後更新」＝最後被寫入的**（推進度、加日誌、改連結），**不含單純點開看過**。
  否則瀏覽一輪之後卡片會跟著滑鼠亂跳
- **每次開啟一律顯示最後更新的，不要記憶上次選取的**。上次點的可能只是隨手看看

**真正的空狀態**只有零個案件時才出現：空表格 ＋ 明顯的「＋新規案件」。

### 決策 4：原始 wireframe 缺的東西（已補）

- 新增案件的入口（頂列「＋新規案件」）
- 空狀態的定義（見決策 3）
- 進度節點是可操作的，不只是顯示

---

## 實作建議

| 項目 | 建議 |
|---|---|
| 氣泡排列 | **d3-force**。自己寫碰撞會做很久且不穩 |
| 資料儲存 | 本機 JSON 就夠。一個人用、幾十筆，不需要資料庫 |
| 打包成 app | **Tauri**（已有星鑑日誌的經驗，坑踩過了） |
| 氣泡數量 | 設上限。只顯示「進行中＋待開始」，完成的沉進列表。超過二十顆小圓會小到點不到 |
| 介面語言 | 日文 |
| 視覺風格 | 尚未決定。方向是安靜、低對比，讓氣泡區成為唯一的視覺焦點 |

---

## 最重要的一件事

**這個 app 的成敗不在視覺，在會不會持續記。**

記錄類工具的死法都一樣：做得漂亮，用兩週就停。差別只在「記一筆」要花幾秒。

所以**日誌那個常駐的一行輸入框是整個介面最重要的元件**——它是唯一每天都會碰的地方。
不要讓它被擠到角落，不要做成要先點「新增」才展開，不要要求填標題或分類。

打開、打字、Enter。三個動作。

---

## 檔案

- `wireframe.html` — 可互動的 wireframe，含編號規格說明。截圖給 Claude Design 當參考圖用
- `claude-design-prompt.md` — 可直接複製貼進 Claude Design 的英文 prompt
- `SPEC.md` — 本檔
