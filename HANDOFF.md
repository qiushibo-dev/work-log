# Work Log — 引き継ぎメモ（v0.1.0 → v0.1.1）

2026-08-14 更新。初版を別マシンで見直し、Tauri アプリとして成立させるための修正を入れた。
**このファイルを先に読んでから作業を始めること。**

---

## まず、初版で正しく守られていたこと

これらは意図的な設計判断なので、**「改善」と称して元に戻さないこと**。
理由はすべて `SPEC.md` の「決策紀錄」にある。

- **気泡にプログレスリングを付けていない** — サイズと濃さの2軸で足りる。リングは却下済み
- **濃さは進捗とともに濃くなる**（薄→濃）— 逆方向は却下済み。理由は SPEC.md 決策 2
- **未選択時は「最終更新の案件」を出す** — 「気泡をクリックしてください」型の空状態は却下済み
- サイズ・濃さの計算式が仕様どおり

---

## 初版で抜けていた4点（修正済み）

### 1. Tauri プラグインが一切入っていなかった（最重要）

`src-tauri/capabilities/default.json` の permissions が `core:default` のみ、
`Cargo.toml` にも `fs` / `dialog` プラグインが無かった。

つまり **dmg にした時点で、アプリなのにファイルもダイアログも扱えない状態**だった。
フロント側をいくら正しく書いても動かない。

修正：

```toml
# src-tauri/Cargo.toml
tauri-plugin-fs = "2"
tauri-plugin-dialog = "2"
```

```rust
// src-tauri/src/lib.rs
.plugin(tauri_plugin_fs::init())
.plugin(tauri_plugin_dialog::init())
```

```json
// src-tauri/capabilities/default.json
"permissions": [
  "core:default", "dialog:default",
  "fs:allow-appdata-read-recursive",
  "fs:allow-appdata-write-recursive",
  "fs:allow-appdata-meta-recursive"
]
```

### 2. `confirm()` を同期で呼んでいた

**Tauri の WebView では `confirm()` は async。** 同期で使うと戻り値が undefined になり、
「完了」「削除」が無反応、あるいは確認なしで実行される。

3箇所（`finish()` / 案件削除 / タグ削除）にあった。

修正：`ask()` でラップし、呼び出し側を `await` に。
Tauri が無い環境（ブラウザ直開き）では `window.confirm` にフォールバックするので両方で動く。

```js
async function ask(msg){
  const t = TA();
  if(t?.dialog?.confirm){ ... return await t.dialog.confirm(msg, {...}); }
  return window.confirm(msg);
}
```

click ハンドラも `async e => {...}` に変更済み。

### 3-b. 【v0.1.4】JSON ファイルが一度も書けていなかった

会社側マシンのレビューで発覚。`~/Library/Application Support/com.shihbo.worklog/`
が存在せず、書き込みが毎回 ENOENT で落ちていた。

**原因**：`writeTextFile` は親ディレクトリを作らない。AppData のディレクトリは
初回起動時点では存在しないため、必ず先に `mkdir` が要る。

**より悪かった点**：失敗を `console.warn` だけで握りつぶしていた。
release ビルドには devtools が無いので、**数十回の失敗が完全に無音**だった。
localStorage が動いていたため、表面上は何の問題も無いように見えていた。

**修正**：
- 書き込み前に `mkdir('', { baseDir: AppData, recursive: true })`。
  空文字を受けつけない版のために `appDataDir()` からの絶対パスで再試行するフォールバックも用意
- **設定画面の「保存先」に実際の状態を出す**。書けていれば実パス、
  失敗していれば赤字でエラー内容。沈黙する失敗を二度と作らないため

**教訓**：ユーザーに見えないところで失敗するくらいなら、крash した方がまだいい。
握りつぶすなら必ず UI に出口を用意する。

### 3. 保存が localStorage のみだった

仕様は「ローカル JSON ファイル」。localStorage は WebView の内部領域なので、
**ユーザーがバックアップできず、アプリの再署名やバージョン変更で消える可能性がある。**
About 画面も "This browser (localStorage)" と表示しており、これはウェブの発想。

修正：**二重化**した。

- `localStorage` — 同期・即時。保険として残す
- `work-tracker.json` — 実体。400ms のデバウンスで AppData に書く
- 起動時：ファイルがあればそちらを正とする（`loadFromFile()`）

保存先：

```
~/Library/Application Support/com.shihbo.worklog/work-tracker.json
```

将来の自動記録（ファイル監視でログを自動生成）にも、この経路が前提になる。
そのためログには `source:'manual'` フィールドを追加済み。自動記録側は `'auto'` を入れる。

### 4. 衝突が本物ではなかった

初版は wireframe に入っていた**一回だけ解く緩和アルゴリズム**をそのまま採用していた。
位置を一度計算したら固定で、気泡は CSS アニメーションでその場で揺れているだけ。
仕様には d3-force と書いてあった。

修正：毎フレーム解く物理ループに置き換え（`tick()`）。

- 気泡同士が実際に押し合う
- 減衰・境界での軽い反発・微細な揺らぎ
- **ドラッグ可能**。掴んで動かすと周りが押しのけられ、離すと慣性が残る
- 5px 以上動いたらクリック扱いにしない（誤操作防止）

#### 気泡サイズの意味（誤読しやすいので明記）

サイズは「投入した労力」ではなく **「その案件が今どれだけ頭を占めているか」**。
制作者本人の言葉では、設計は中盤が一番しんどく、終盤は機械的な作業が残るだけなので
重要度が下がる。だから正規分布の形になる。

濃さは別軸で「どこまで進んだか」。両方が同時に減ると
「着手直後」と「完了間近」が区別できなくなる（どちらも小さく薄い）ため、濃さは単調増加。

- 小さく薄い → 始まったばかり
- 大きく中間 → 今まさに頭を使っている
- 小さく濃い → もう終わる、安心していい

d3-force ではなく自前実装にした理由：単一 HTML ファイルの構成を保つため。
外部ライブラリを CDN で読むとオフラインで壊れ、インライン化するとファイルが肥大する。
12件規模なら自前の方が軽い。

### 5. サイドバーの区切り線が途中で切れていた

`.side` が `position:sticky` かつ `align-self:start` のため、
**自分のコンテンツ分の高さしか持たない**。リストが長いと `border-right` がそこで途切れる。

修正：線を `.lower::before`（`top:0; bottom:0`）に移動。どちらが長くても貫通する。

---

## そのほか今回入れた変更

| 内容 | 詳細 |
|---|---|
| 気泡サイズの調整 | 最小 76px → **42px**、山場は 160px 維持。コントラストが 2.1倍 → 3.8倍に |
| サイズを可変パラメータ化 | `let SIZE = { base, span, floor }`。console から書き換えて `renderAll()` で即反映 |
| 文字サイズが直径に追従 | 52px 以上で名称表示（従来は 78px 以上）。8.5px〜13px を線形補間、2行でクランプ |
| スクリーンセーバー | 無操作 5分（設定で 0/5/15分）で気泡だけの全画面へ。セーバー中は減衰を 0.935→0.972 に緩めてゆっくり漂わせる |
| 初期サンプル | Project A〜E の5件、**全て step 0**。同じ最小サイズ・最も淡い状態から始まる |
| About から社名を削除 | 個人の制作物のため |

### 初期サンプルを全て step 0 にした理由

初版は 1,3,5,6,8 とバラバラで、開いた瞬間は綺麗だが
**その大きさがどこから来たのかユーザーには分からない。**

全て同じ最小サイズなら、最初の進捗ノードを押した瞬間に
「気泡が育って濃くなる」のを自分の操作で目撃できる。ルールの説明が要らなくなる。

---

## 触るときの注意

**調整用のパラメータ**（値を変えるだけ。構造は触らなくていい）

```js
let SIZE = { base:16, span:144, floor:0.18 };   // 気泡サイズの曲線
const damp = saverOn ? 0.972 : 0.935;            // 物理の減衰
b.vx += (cx-b.x)*0.0007 + (Math.random()-0.5)*0.055;  // 中心力と揺らぎ
```

**やらないこと**

- 気泡にリング・枠線・パーセント表示を足さない（SPEC.md 決策 1）
- 分類色・締切バッジなど情報量を増やす要素を足さない。
  気泡ゾーンは「情報を読む場所」ではなく「状態を感じる場所」
- `renderAll()` は DOM を作り直すので、物理の位置は `tick()` 側が持つ。
  レンダリング側で `left/top` を書かないこと

**ビルド**

```bash
npx --yes @tauri-apps/cli@^2 build
```

`package.json` は無く、`beforeBuildCommand` が `app.html` を `dist/index.html` にコピーする構成。

---

## 検証すべき5点（dmg をインストールしたあと）

1. 案件を追加 → **Cmd+Q で完全終了** → 再起動 → データが残っているか
2. `~/Library/Application Support/com.shihbo.worklog/work-tracker.json` が生成されているか
3. 「案件を削除」で **macOS ネイティブのダイアログ**が出るか（ウェブ風のものではなく）
4. 進捗ノードを押して、左の気泡が即座に大きさと濃さを変えるか
5. 気泡をドラッグして、周りが押しのけられるか

1 と 2 が通れば「動くプロトタイプ」ではなく「使えるツール」になっている。
