# Babos — 引き継ぎメモ

最終更新：2026-08-14（v0.1.6）

初版から v0.1.6 までに直したものと、その過程で分かったことの記録。
**作業を始める前にこのファイルを読むこと。** 同じ穴を二度掘らないために書いてある。

設計判断そのものの経緯は `SPEC.md`（繁体字中国語）にある。

---

## 1. 触ってはいけないところ

これらは意図的な判断であって、抜けや手抜きではない。
**「改善」と称して元に戻さないこと。** 理由はすべて `SPEC.md` の決策紀錄にある。

- **気泡にプログレスリング・枠線・パーセント表示を付けない** — サイズと濃さの2軸で足りている。却下済み
- **濃さは進捗とともに濃くなる**（薄→濃）。逆方向は却下済み（SPEC.md 決策 2）
- **未選択時は「最終更新の案件」を出す**。「気泡をクリックしてください」型の空状態は却下済み（決策 3）
- **分類色・締切バッジなど情報量を増やす要素を足さない**。
  気泡ゾーンは「情報を読む場所」ではなく「状態を感じる場所」
- **`renderAll()` の中で `left/top` を書かない**。DOM を作り直す側と位置を持つ側（`tick()`）を混ぜない
- **Tauri の `setFullscreen` を呼ばない**（→ 3.4）

### 気泡サイズの意味（誤読しやすいので明記）

サイズは「投入した労力」ではなく **「その案件が今どれだけ頭を占めているか」**。
制作者本人の言葉では、設計は中盤が一番しんどく、終盤は機械的な作業が残るだけなので
重要度が下がる。だから正規分布の形になる。

濃さは別軸で「どこまで進んだか」。両方が同時に減ると
「着手直後」と「完了間近」が区別できなくなる（どちらも小さく薄い）ため、濃さは単調増加。

| 見た目 | 意味 |
|---|---|
| 小さく薄い | 始まったばかり |
| 大きく中間 | 今まさに頭を使っている |
| 小さく濃い | もう終わる、安心していい |

---

## 2. v0.1.1 — 初版が Tauri アプリとして成立していなかった

### 2.1 プラグインが一切入っていなかった（最重要）

`src-tauri/capabilities/default.json` の permissions が `core:default` のみ、
`Cargo.toml` にも `fs` / `dialog` プラグインが無かった。

つまり **dmg にした時点で、アプリなのにファイルもダイアログも扱えない状態**だった。
フロント側をいくら正しく書いても動かない。

```toml
# src-tauri/Cargo.toml
tauri-plugin-fs = "2"
tauri-plugin-dialog = "2"
```

```rust
// src-tauri/src/lib.rs
.plugin(tauri_plugin_fs::init())
.plugin(tauri_plugin_dialog::init())
```

```json
// src-tauri/capabilities/default.json
"permissions": [
  "core:default", "dialog:default",
  "fs:allow-appdata-read-recursive",
  "fs:allow-appdata-write-recursive",
  "fs:allow-appdata-meta-recursive"
]
```

### 2.2 `confirm()` を同期で呼んでいた

**Tauri の WebView では `confirm()` は async。** 同期で使うと戻り値が undefined になり、
「完了」「削除」が無反応、あるいは確認なしで実行される。3箇所にあった。

`ask()` でラップし、呼び出し側を `await` に。Tauri が無い環境（ブラウザ直開き）では
`window.confirm` にフォールバックするので両方で動く。click ハンドラも `async e => {…}` に変更済み。

### 2.3 保存が localStorage のみだった

仕様は「ローカル JSON ファイル」。localStorage は WebView の内部領域なので、
**ユーザーがバックアップできず、アプリの再署名やバージョン変更で消える可能性がある。**

二重化した。`localStorage` は同期・即時で保険として残し、実体は
`work-tracker.json` を 400ms デバウンスで AppData に書く。起動時はファイルを正とする。

将来の自動記録（ファイル監視でログを自動生成）にも、この経路が前提になる。
そのためログには `source:'manual'` フィールドを追加済み。自動記録側は `'auto'` を入れる。

### 2.4 衝突が本物ではなかった

初版は wireframe に入っていた**一回だけ解く緩和アルゴリズム**をそのまま採用していた。
位置を一度計算したら固定で、気泡は CSS アニメーションでその場で揺れているだけ。
仕様には d3-force と書いてあった。

毎フレーム解く物理ループ（`tick()`）に置き換え。気泡同士が実際に押し合い、
ドラッグでき、離すと慣性が残る。5px 以上動いたらクリック扱いにしない。

**d3-force ではなく自前実装にした理由**：単一 HTML ファイルの構成を保つため。
外部ライブラリを CDN で読むとオフラインで壊れ、インライン化するとファイルが肥大する。
12件規模なら自前の方が軽い。

### 2.5 サイドバーの区切り線が途中で切れていた

`.side` が `position:sticky` かつ `align-self:start` のため、
**自分のコンテンツ分の高さしか持たない**。リストが長いと `border-right` がそこで途切れる。

線を `.lower::before`（`top:0; bottom:0`）に移動。どちらが長くても貫通する。

### 2.6 そのほか

| 内容 | 詳細 |
|---|---|
| 気泡サイズの調整 | 最小 76px → **42px**、山場は 160px 維持。コントラストが 2.1倍 → 3.8倍に |
| サイズを可変パラメータ化 | `let SIZE = { base, span, floor }`。console から書き換えて `renderAll()` で即反映 |
| 文字サイズが直径に追従 | 52px 以上で名称表示。8.5px〜13px を線形補間、2行でクランプ |
| スクリーンセーバー | 無操作 5分（設定で 0/5/15分）で気泡だけの画面へ。セーバー中は減衰を 0.935→0.972 に緩める |
| 初期サンプル | Project A〜E の5件、**全て step 0** |
| About から社名を削除 | 個人の制作物のため |

**初期サンプルを全て step 0 にした理由**：初版は 1,3,5,6,8 とバラバラで、開いた瞬間は
綺麗だが**その大きさがどこから来たのかユーザーには分からない。** 全て同じ最小サイズなら、
最初の進捗ノードを押した瞬間に「気泡が育って濃くなる」のを自分の操作で目撃できる。

---

## 3. v0.1.2 – v0.1.3 — スクリーンセーバーのクラッシュ地獄

**この節がこのファイルで一番重要。** 半日をここに溶かした。

### 3.1 何が起きたか

セーバーを「本物のフルスクリーン」にしようとして `window.setFullscreen(true)` を導入。
開発機では動いたが、**dmg をインストールした実機ではセーバー突入と同時にアプリが落ちる。**

以下を順に試して、**すべて落ちた**：

1. `setFullscreen(true)` をそのまま呼ぶ → クラッシュ
2. 撤回 → 落ちなくなる（原因の切り分けは取れた）
3. CSS 側の真因と思われた `.upper` の block 化を修正した上で再導入 → **またクラッシュ**
4. `setTimeout` で遅延させ、状態フラグで多重呼び出しを防いだ上で再導入 → **またクラッシュ**

### 3.2 結論

**この環境では `setFullscreen` を呼ばない。** 3回試して3回落ちたので、
CSS やタイミングの問題ではなく API 自体が不安定と判断した。

フルスクリーンで見たい場合は、**アプリのウィンドウを先に macOS 側のフルスクリーン
（緑ボタン / Cmd+Ctrl+F）にしておけば**、セーバーは CSS でその全面を埋めるので同じ結果になる。

### 3.3 副産物として入れた対策（これらは残す価値がある）

- **切替前後 900ms は物理の DOM 更新を止める**（`physPaused`）。
  レイアウトが総入れ替えになる最中に毎フレーム12個の位置を書き換えると WebView が耐えられない
- **ウィンドウが前面に無いときは数えない**。裏に回ったアプリがセーバーに入っても意味がなく、
  戻ってきた瞬間に画面が変わっていて驚くだけ。`blur` / `visibilitychange` で止める
- セーバーの CSS は**最小構成のグリッド**に留める。凝った compositing を足すと再発しかねない

### 3.4 ここから得た一般則

> **WKWebView（Tauri/macOS）は Chromium と同じ挙動をしない。**
> ブラウザで確認できたことは「実機でも動く」の根拠にならない。
> 特にウィンドウ制御 API と重い CSS compositing は、必ず dmg をインストールして確かめる。

### 3.5 ついでに入れたもの

- ad-hoc 署名（`"signingIdentity": "-"`）を bundle 全体に適用。
  それでも Gatekeeper には引っかかるので、初回は `xattr -dr com.apple.quarantine` が要る
- アプリ名を **Babos** に変更（v0.1.3）

---

## 4. v0.1.4 — JSON ファイルが一度も書けていなかった

会社側マシンでのレビューで発覚。
`~/Library/Application Support/com.shihbo.worklog/` が存在せず、書き込みが毎回 ENOENT で落ちていた。

**原因**：`writeTextFile` は親ディレクトリを作らない。AppData のディレクトリは
初回起動時点では存在しないため、必ず先に `mkdir` が要る。

**より悪かった点**：失敗を `console.warn` だけで握りつぶしていた。
release ビルドには devtools が無いので、**数十回の失敗が完全に無音**だった。
localStorage が動いていたため、表面上は何の問題も無いように見えていた。

修正：

- 書き込み前に `mkdir('', { baseDir: AppData, recursive: true })`。
  空文字を受けつけない版のために `appDataDir()` からの絶対パスで再試行するフォールバックも用意
- **設定画面の「保存先」に実際の状態を出す**。書けていれば実パス、失敗していれば赤字でエラー内容

> **教訓**：ユーザーに見えないところで失敗するくらいなら、クラッシュした方がまだいい。
> 握りつぶすなら必ず UI に出口を用意する。

---

## 5. v0.1.5 — 保存先の表示にスラッシュが抜けていた

`appDataDir()` の戻り値に末尾スラッシュが有る版と無い版がある。
連結時に補うようにした。表示だけの問題で、書き込み自体は正常だった。

---

## 6. v0.1.6 — 起動直後にファイルを潰しうる競合を塞いだ

コードを通しで読み直して見つけたもの。実害は出ていないが、条件が揃うと記録が消える。

**何が起きうるか**：起動シーケンスはこうなっていた。

```
1. load()             localStorage から state を起こす
2. applyTheme/applyLang → save() → writeFile() が 400ms のタイマーを張る
3. loadFromFile()     JSON ファイルを非同期で読み始める（こちらが正）
```

3 が 400ms 以内に終われば問題ない。だが**読み込みが 400ms を超えると、
2 で張ったタイマーが先に発火して、localStorage の古い内容でファイルを上書きする。**

別マシンから `work-tracker.json` を持ってきた直後や、Gatekeeper のスキャンで
初回起動の IO が遅いときに踏む。**「ファイルが正」という設計意図がそこだけ裏返る。**

**修正**：`booted` フラグでゲートを張り、`loadFromFile()` が終わるまで
`writeFile()` を素通りさせる。読み終わってからゲートを開け、確定した state を一度だけ書き戻す。
localStorage への同期書き込みはゲートの対象外なので、保険としての即時性は落ちていない。

ついでに、ブラウザ起動時に `sortBy` が復元されていなかったのも直した
（`loadFromFile()` の中にしか復元処理が無かった）。

---

## 7. 触るときの注意

### 調整用のパラメータ（値を変えるだけ。構造は触らなくていい）

```js
let SIZE = { base:16, span:144, floor:0.18 };   // 気泡サイズの曲線
const damp = saverOn ? 0.972 : 0.935;            // 物理の減衰
b.vx += (cx-b.x)*0.0007 + (Math.random()-0.5)*0.055;  // 中心力と揺らぎ
```

`SIZE` は console から書き換えて `renderAll()` を呼べば即反映される。
`floor` を下げるほど「着手直後」と「山場」の差が開く。

### ビルド

```bash
npx --yes @tauri-apps/cli@^2 build
```

`package.json` は無く、`beforeBuildCommand` が `app.html` を `dist/index.html` に
コピーする構成。**編集対象は常にルートの `app.html`。**

バージョンは `tauri.conf.json` と `app.html` の `APP_VERSION` の**2箇所**にある。両方直す。

---

## 8. dmg をインストールしたあとに確認すること

1. 案件を追加 → **Cmd+Q で完全終了** → 再起動 → データが残っているか
2. `~/Library/Application Support/com.shihbo.worklog/work-tracker.json` が生成されているか
3. 設定画面の「保存先」が**実パスを表示**しているか（赤字なら書けていない）
4. 「案件を削除」で **macOS ネイティブのダイアログ**が出るか（ウェブ風のものではなく）
5. 進捗ノードを押して、左の気泡が即座に大きさと濃さを変えるか
6. 気泡をドラッグして、周りが押しのけられるか
7. **セーバーに入って落ちないか**（設定でセーバーを 0 分にはせず、5分のまま放置して確認）

1・2・3 が通れば「動くプロトタイプ」ではなく「使えるツール」になっている。

---

## 9. 未検証・未解決

- **`file://` のリンクが Tauri WebView で実際に開くか未確認。**
  `target="_blank"` は WebView 側で握り潰される可能性がある。
  駄目なら `opener` プラグイン（`tauri-plugin-opener`）が要る
- 完了アニメーションの2秒の間にアプリを終了すると `done` が書かれない
